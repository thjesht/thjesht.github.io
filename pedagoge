<!DOCTYPE html>

<html lang="sq">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Modul 2 — Sistemi i Pedagogëve</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Mono:wght@400;500&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --navy: #0f1e3c;
    --navy-mid: #1a2f56;
    --navy-light: #243d6e;
    --gold: #c8963e;
    --gold-light: #e8b86d;
    --cream: #f5f0e8;
    --white: #ffffff;
    --muted: #8fa0bb;
    --success: #2a9d6e;
    --danger: #c0392b;
    --card-bg: #162344;
    --border: rgba(200,150,62,0.25);
  }

- { margin: 0; padding: 0; box-sizing: border-box; }

body {
background: var(–navy);
color: var(–cream);
font-family: ‘Outfit’, sans-serif;
min-height: 100vh;
position: relative;
overflow-x: hidden;
}

body::before {
content: ‘’;
position: fixed;
top: -200px; right: -200px;
width: 600px; height: 600px;
border-radius: 50%;
background: radial-gradient(circle, rgba(200,150,62,0.08) 0%, transparent 70%);
pointer-events: none;
}

/* HEADER */
header {
background: linear-gradient(135deg, var(–navy-mid) 0%, var(–navy) 100%);
border-bottom: 1px solid var(–border);
padding: 0 2rem;
position: sticky; top: 0; z-index: 100;
backdrop-filter: blur(10px);
}

.header-inner {
max-width: 1100px;
margin: 0 auto;
display: flex;
align-items: center;
justify-content: space-between;
height: 64px;
}

.logo-area {
display: flex;
align-items: center;
gap: 12px;
}

.logo-badge {
width: 38px; height: 38px;
background: linear-gradient(135deg, var(–gold), var(–gold-light));
border-radius: 8px;
display: flex; align-items: center; justify-content: center;
font-family: ‘DM Serif Display’, serif;
font-size: 18px;
color: var(–navy);
font-weight: bold;
}

.logo-text {
display: flex; flex-direction: column;
}

.logo-text .sys { font-size: 10px; text-transform: uppercase; letter-spacing: 2px; color: var(–gold); font-weight: 500; }
.logo-text .mod { font-size: 16px; font-weight: 600; color: var(–white); }

.breadcrumb {
font-size: 12px;
color: var(–muted);
font-family: ‘DM Mono’, monospace;
}
.breadcrumb span { color: var(–gold); }

.datetime-display {
font-family: ‘DM Mono’, monospace;
font-size: 12px;
color: var(–muted);
text-align: right;
}
.datetime-display .time { font-size: 18px; color: var(–cream); font-weight: 500; }

/* TABS */
.tabs {
max-width: 1100px;
margin: 2rem auto 0;
padding: 0 2rem;
display: flex;
gap: 4px;
}

.tab {
padding: 10px 24px;
border-radius: 8px 8px 0 0;
cursor: pointer;
font-size: 14px;
font-weight: 500;
transition: all 0.2s;
border: 1px solid transparent;
border-bottom: none;
user-select: none;
}

.tab.active {
background: var(–card-bg);
color: var(–gold-light);
border-color: var(–border);
}

.tab:not(.active) {
background: rgba(255,255,255,0.04);
color: var(–muted);
}

.tab:not(.active):hover {
background: rgba(255,255,255,0.08);
color: var(–cream);
}

.tab-icon { margin-right: 6px; }

/* MAIN */
main {
max-width: 1100px;
margin: 0 auto;
padding: 0 2rem 4rem;
}

.tab-content {
background: var(–card-bg);
border: 1px solid var(–border);
border-radius: 0 12px 12px 12px;
padding: 2rem;
display: none;
animation: fadeIn 0.3s ease;
}

.tab-content.active { display: block; }

@keyframes fadeIn {
from { opacity: 0; transform: translateY(8px); }
to { opacity: 1; transform: translateY(0); }
}

/* SECTION TITLE */
.section-title {
display: flex;
align-items: center;
gap: 12px;
margin-bottom: 1.5rem;
padding-bottom: 1rem;
border-bottom: 1px solid var(–border);
}

.section-title h2 {
font-family: ‘DM Serif Display’, serif;
font-size: 22px;
color: var(–cream);
}

.section-title .badge {
background: rgba(200,150,62,0.15);
color: var(–gold-light);
padding: 3px 10px;
border-radius: 20px;
font-size: 12px;
border: 1px solid var(–border);
font-family: ‘DM Mono’, monospace;
}

/* FORM */
.form-grid {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 1.2rem;
margin-bottom: 1.5rem;
}

.form-group {
display: flex;
flex-direction: column;
gap: 6px;
}

.form-group.full { grid-column: 1 / -1; }

label {
font-size: 12px;
text-transform: uppercase;
letter-spacing: 1.5px;
color: var(–muted);
font-weight: 500;
}

.label-required::after {
content: ’ *’;
color: var(–gold);
}

input, select, textarea {
background: rgba(255,255,255,0.05);
border: 1px solid rgba(200,150,62,0.2);
border-radius: 8px;
padding: 10px 14px;
color: var(–cream);
font-family: ‘Outfit’, sans-serif;
font-size: 14px;
transition: all 0.2s;
outline: none;
}

input:focus, select:focus, textarea:focus {
border-color: var(–gold);
background: rgba(200,150,62,0.06);
box-shadow: 0 0 0 3px rgba(200,150,62,0.1);
}

select option { background: var(–navy-mid); color: var(–cream); }

textarea { resize: vertical; min-height: 80px; }

/* BUTTONS */
.btn-row {
display: flex;
gap: 10px;
flex-wrap: wrap;
}

.btn {
display: inline-flex;
align-items: center;
gap: 8px;
padding: 10px 22px;
border-radius: 8px;
border: none;
cursor: pointer;
font-family: ‘Outfit’, sans-serif;
font-size: 14px;
font-weight: 500;
transition: all 0.2s;
}

.btn-primary {
background: linear-gradient(135deg, var(–gold), var(–gold-light));
color: var(–navy);
}

.btn-primary:hover {
transform: translateY(-1px);
box-shadow: 0 6px 20px rgba(200,150,62,0.35);
}

.btn-outline {
background: transparent;
color: var(–gold-light);
border: 1px solid var(–border);
}

.btn-outline:hover {
background: rgba(200,150,62,0.1);
}

.btn-danger {
background: rgba(192,57,43,0.15);
color: #e74c3c;
border: 1px solid rgba(192,57,43,0.3);
}

.btn-danger:hover { background: rgba(192,57,43,0.25); }

/* TABLE */
.table-wrap {
overflow-x: auto;
border-radius: 10px;
border: 1px solid var(–border);
margin-top: 1.5rem;
}

table {
width: 100%;
border-collapse: collapse;
font-size: 13.5px;
}

thead {
background: rgba(200,150,62,0.1);
}

thead th {
padding: 12px 16px;
text-align: left;
font-size: 11px;
text-transform: uppercase;
letter-spacing: 1.5px;
color: var(–gold);
font-weight: 600;
border-bottom: 1px solid var(–border);
white-space: nowrap;
}

tbody tr {
border-bottom: 1px solid rgba(255,255,255,0.05);
transition: background 0.15s;
}

tbody tr:last-child { border-bottom: none; }
tbody tr:hover { background: rgba(255,255,255,0.04); }

td {
padding: 12px 16px;
color: var(–cream);
vertical-align: middle;
}

.chip {
display: inline-block;
padding: 2px 10px;
border-radius: 20px;
font-size: 11px;
font-weight: 500;
font-family: ‘DM Mono’, monospace;
}

.chip-blue { background: rgba(52,152,219,0.15); color: #5dade2; border: 1px solid rgba(52,152,219,0.25); }
.chip-green { background: rgba(42,157,110,0.15); color: #2ecc97; border: 1px solid rgba(42,157,110,0.25); }
.chip-gold { background: rgba(200,150,62,0.15); color: var(–gold-light); border: 1px solid var(–border); }

.empty-state {
text-align: center;
padding: 3rem;
color: var(–muted);
}

.empty-state .icon { font-size: 48px; margin-bottom: 1rem; opacity: 0.4; }
.empty-state p { font-size: 14px; }

/* XML OUTPUT */
.xml-box {
background: #0a1628;
border: 1px solid var(–border);
border-radius: 10px;
padding: 1.5rem;
margin-top: 1rem;
position: relative;
}

.xml-box pre {
font-family: ‘DM Mono’, monospace;
font-size: 13px;
color: #a8d8a8;
line-height: 1.7;
overflow-x: auto;
white-space: pre;
}

.xml-box .xml-tag { color: #7fc3e8; }
.xml-box .xml-attr { color: var(–gold-light); }
.xml-box .xml-val { color: #a8d8a8; }

.copy-btn {
position: absolute;
top: 12px; right: 12px;
background: rgba(200,150,62,0.15);
color: var(–gold-light);
border: 1px solid var(–border);
border-radius: 6px;
padding: 5px 12px;
font-size: 12px;
cursor: pointer;
font-family: ‘Outfit’, sans-serif;
transition: all 0.2s;
}

.copy-btn:hover { background: rgba(200,150,62,0.25); }

/* TOAST */
.toast {
position: fixed;
bottom: 2rem; right: 2rem;
background: var(–navy-mid);
border: 1px solid var(–border);
border-left: 3px solid var(–gold);
border-radius: 8px;
padding: 12px 20px;
font-size: 14px;
color: var(–cream);
box-shadow: 0 8px 30px rgba(0,0,0,0.4);
transform: translateX(200%);
transition: transform 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275);
z-index: 999;
max-width: 300px;
}

.toast.show { transform: translateX(0); }
.toast.success { border-left-color: var(–success); }
.toast.error { border-left-color: var(–danger); }

/* STATS BAR */
.stats-bar {
display: grid;
grid-template-columns: repeat(4, 1fr);
gap: 1rem;
margin-bottom: 1.5rem;
}

.stat-card {
background: rgba(255,255,255,0.04);
border: 1px solid var(–border);
border-radius: 10px;
padding: 1rem 1.2rem;
}

.stat-card .label { font-size: 11px; text-transform: uppercase; letter-spacing: 1.5px; color: var(–muted); }
.stat-card .value { font-size: 26px; font-weight: 600; color: var(–gold-light); font-family: ‘DM Serif Display’, serif; margin-top: 4px; }

/* FILTER ROW */
.filter-row {
display: flex;
gap: 10px;
margin-bottom: 1rem;
flex-wrap: wrap;
}

.filter-row input, .filter-row select {
flex: 1;
min-width: 160px;
}

/* SCROLL */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(200,150,62,0.3); border-radius: 3px; }
</style>

</head>
<body>

<header>
  <div class="header-inner">
    <div class="logo-area">
      <div class="logo-badge">M2</div>
      <div class="logo-text">
        <span class="sys">Sistemi Akademik</span>
        <span class="mod">Modul 2 — Pedagogët</span>
      </div>
    </div>
    <div class="breadcrumb">Sistemi 1 / <span>Modul 2</span> / S.Pedagogëve</div>
    <div class="datetime-display">
      <div class="time" id="clock">--:--:--</div>
      <div id="date-display">Duke u ngarkuar...</div>
    </div>
  </div>
</header>

<div class="tabs">
  <div class="tab active" onclick="switchTab('add')">
    <span class="tab-icon">📝</span> Shto Pedagog
  </div>
  <div class="tab" onclick="switchTab('list')">
    <span class="tab-icon">📋</span> Lexim / Afishim
  </div>
  <div class="tab" onclick="switchTab('xml')">
    <span class="tab-icon">📄</span> Gjenerimi XML
  </div>
</div>

<main>

  <!-- TAB 1: SHTO -->

  <div class="tab-content active" id="tab-add">
    <div class="section-title">
      <h2>Regjistro Pedagog të Ri</h2>
      <span class="badge">INPUT FORM</span>
    </div>

```
<div class="form-grid">
  <div class="form-group">
    <label class="label-required">ID Pedagog</label>
    <input type="text" id="f-id" placeholder="P-0001" />
  </div>
  <div class="form-group">
    <label class="label-required">Nr. Personelit</label>
    <input type="text" id="f-nr" placeholder="NP-2024-001" />
  </div>
  <div class="form-group">
    <label class="label-required">Emri</label>
    <input type="text" id="f-emri" placeholder="Arben" />
  </div>
  <div class="form-group">
    <label class="label-required">Mbiemri</label>
    <input type="text" id="f-mbiemri" placeholder="Berisha" />
  </div>
  <div class="form-group">
    <label class="label-required">Titulli Akademik</label>
    <select id="f-titulli">
      <option value="">-- Zgjidh --</option>
      <option>Prof. Dr.</option>
      <option>Dr.</option>
      <option>MSc.</option>
      <option>Asistent</option>
      <option>Lektor</option>
    </select>
  </div>
  <div class="form-group">
    <label class="label-required">Fakulteti</label>
    <select id="f-fakulteti">
      <option value="">-- Zgjidh --</option>
      <option>Fakulteti i Shkencave Kompjuterike</option>
      <option>Fakulteti Ekonomik</option>
      <option>Fakulteti Juridik</option>
      <option>Fakulteti i Inxhinierisë</option>
      <option>Fakulteti i Mjekësisë</option>
    </select>
  </div>
  <div class="form-group">
    <label class="label-required">Departamenti</label>
    <input type="text" id="f-dept" placeholder="p.sh. Informatikë" />
  </div>
  <div class="form-group">
    <label class="label-required">Viti Akademik</label>
    <select id="f-viti">
      <option>2024-2025</option>
      <option>2025-2026</option>
      <option>2026-2027</option>
    </select>
  </div>
  <div class="form-group full">
    <label>Lëndët që Mëson</label>
    <textarea id="f-lendet" placeholder="p.sh. Bazat e Programimit, Algoritmet dhe Strukturat e të Dhënave, ..."></textarea>
  </div>
  <div class="form-group">
    <label>Kredite Totale</label>
    <input type="number" id="f-kredite" placeholder="p.sh. 30" min="0" max="200" />
  </div>
  <div class="form-group">
    <label>Email Institucional</label>
    <input type="email" id="f-email" placeholder="pedagog@uni.edu.al" />
  </div>
</div>

<div class="btn-row">
  <button class="btn btn-primary" onclick="shto()">✚ Shto Pedagogun</button>
  <button class="btn btn-outline" onclick="resetForm()">↺ Pastro Formularin</button>
</div>
```

  </div>

  <!-- TAB 2: LEXIM -->

  <div class="tab-content" id="tab-list">
    <div class="section-title">
      <h2>Lista e Pedagogëve</h2>
      <span class="badge" id="counter-badge">0 rekorde</span>
    </div>

```
<div class="stats-bar">
  <div class="stat-card">
    <div class="label">Gjithsej Pedagogë</div>
    <div class="value" id="stat-total">0</div>
  </div>
  <div class="stat-card">
    <div class="label">Departamente</div>
    <div class="value" id="stat-dept">0</div>
  </div>
  <div class="stat-card">
    <div class="label">Kredite Totale</div>
    <div class="value" id="stat-kredite">0</div>
  </div>
  <div class="stat-card">
    <div class="label">Viti Aktiv</div>
    <div class="value" style="font-size:16px;margin-top:8px;">2024-25</div>
  </div>
</div>

<div class="filter-row">
  <input type="text" id="filter-search" placeholder="🔍  Kërko sipas emrit ose ID..." oninput="filterTable()" />
  <select id="filter-dept" onchange="filterTable()">
    <option value="">Të gjitha Departamentet</option>
  </select>
</div>

<div class="table-wrap">
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Nr. Personeli</th>
        <th>Emri & Mbiemri</th>
        <th>Titulli</th>
        <th>Departamenti</th>
        <th>Kredite</th>
        <th>Viti Akad.</th>
        <th>Veprime</th>
      </tr>
    </thead>
    <tbody id="table-body">
      <tr>
        <td colspan="8">
          <div class="empty-state">
            <div class="icon">🎓</div>
            <p>Nuk ka pedagogë të regjistruar akoma.<br>Shko te skeda <strong>Shto Pedagog</strong>.</p>
          </div>
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

  </div>

  <!-- TAB 3: XML -->

  <div class="tab-content" id="tab-xml">
    <div class="section-title">
      <h2>Gjenerimi i Skedarit XML</h2>
      <span class="badge">EKSPORT</span>
    </div>

```
<div class="btn-row" style="margin-bottom:1.5rem;">
  <button class="btn btn-primary" onclick="generateXML()">⚙ Gjeneroni XML</button>
  <button class="btn btn-outline" onclick="downloadXML()">⬇ Shkarko XML</button>
  <button class="btn btn-outline" onclick="copyXML()">📋 Kopjo XML</button>
</div>

<div id="xml-preview" class="xml-box" style="display:none;">
  <button class="copy-btn" onclick="copyXML()">📋 Kopjo</button>
  <pre id="xml-output"></pre>
</div>

<div id="xml-empty" class="empty-state" style="margin-top:2rem;">
  <div class="icon">📄</div>
  <p>Shtypni butonin <strong>Gjeneroni XML</strong> për të parë skedarin.<br>
  Duhet të keni të paktën 1 pedagog të regjistruar.</p>
</div>
```

  </div>

</main>

<div class="toast" id="toast"></div>

<script>
let pedagoget = [];

// CLOCK
function updateClock() {
  const now = new Date();
  document.getElementById('clock').textContent = now.toLocaleTimeString('sq-AL');
  document.getElementById('date-display').textContent = now.toLocaleDateString('sq-AL', {weekday:'long', year:'numeric', month:'long', day:'numeric'});
}
setInterval(updateClock, 1000);
updateClock();

// TABS
function switchTab(name) {
  document.querySelectorAll('.tab').forEach((t,i) => t.classList.remove('active'));
  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
  const tabs = ['add','list','xml'];
  document.querySelectorAll('.tab')[tabs.indexOf(name)].classList.add('active');
  document.getElementById('tab-'+name).classList.add('active');
  if (name === 'list') renderTable(pedagoget);
  if (name === 'xml') { document.getElementById('xml-preview').style.display='none'; document.getElementById('xml-empty').style.display='block'; }
}

// TOAST
function showToast(msg, type='success') {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.className = 'toast show ' + type;
  setTimeout(() => t.className = 'toast', 3000);
}

// ADD
function shto() {
  const id = document.getElementById('f-id').value.trim();
  const nr = document.getElementById('f-nr').value.trim();
  const emri = document.getElementById('f-emri').value.trim();
  const mbiemri = document.getElementById('f-mbiemri').value.trim();
  const titulli = document.getElementById('f-titulli').value;
  const fakulteti = document.getElementById('f-fakulteti').value;
  const dept = document.getElementById('f-dept').value.trim();
  const viti = document.getElementById('f-viti').value;
  const lendet = document.getElementById('f-lendet').value.trim();
  const kredite = document.getElementById('f-kredite').value;
  const email = document.getElementById('f-email').value.trim();

  if (!id || !emri || !mbiemri || !titulli || !fakulteti || !dept || !nr) {
    showToast('⚠ Plotëso fushat e detyrueshme!', 'error'); return;
  }
  if (pedagoget.find(p => p.id === id)) {
    showToast('⚠ Ekziston tashmë pedagog me këtë ID!', 'error'); return;
  }

  const now = new Date();
  pedagoget.push({ id, nr, emri, mbiemri, titulli, fakulteti, dept, viti, lendet, kredite: kredite || '0', email, ora: now.toLocaleString('sq-AL') });

  showToast('✓ Pedagog u shtua me sukses!');
  resetForm();
  updateStats();
  updateFilterDept();
}

function resetForm() {
  ['f-id','f-nr','f-emri','f-mbiemri','f-dept','f-lendet','f-kredite','f-email'].forEach(id => document.getElementById(id).value = '');
  ['f-titulli','f-fakulteti'].forEach(id => document.getElementById(id).selectedIndex = 0);
}

// TABLE
function renderTable(data) {
  const tbody = document.getElementById('table-body');
  if (data.length === 0) {
    tbody.innerHTML = `<tr><td colspan="8"><div class="empty-state"><div class="icon">🎓</div><p>Nuk ka rezultate.</p></div></td></tr>`;
    return;
  }
  tbody.innerHTML = data.map(p => `
    <tr>
      <td><span class="chip chip-gold">${p.id}</span></td>
      <td><span style="font-family:'DM Mono',monospace;font-size:12px;color:var(--muted)">${p.nr}</span></td>
      <td><strong>${p.titulli} ${p.emri} ${p.mbiemri}</strong></td>
      <td><span class="chip chip-blue">${p.titulli}</span></td>
      <td>${p.dept}<br><small style="color:var(--muted);font-size:11px">${p.fakulteti.split(' ').slice(0,3).join(' ')}...</small></td>
      <td><span class="chip chip-green">${p.kredite} kr.</span></td>
      <td style="font-family:'DM Mono',monospace;font-size:12px">${p.viti}</td>
      <td>
        <button class="btn btn-danger" style="padding:5px 12px;font-size:12px" onclick="fshi('${p.id}')">✕ Fshi</button>
      </td>
    </tr>
  `).join('');

  document.getElementById('counter-badge').textContent = data.length + ' rekorde';
}

function fshi(id) {
  pedagoget = pedagoget.filter(p => p.id !== id);
  updateStats();
  updateFilterDept();
  renderTable(pedagoget);
  showToast('🗑 Pedagog u fshi.', 'error');
}

function filterTable() {
  const q = document.getElementById('filter-search').value.toLowerCase();
  const dept = document.getElementById('filter-dept').value;
  const filtered = pedagoget.filter(p => {
    const matchQ = !q || (p.emri + p.mbiemri + p.id).toLowerCase().includes(q);
    const matchD = !dept || p.dept === dept;
    return matchQ && matchD;
  });
  renderTable(filtered);
}

function updateStats() {
  document.getElementById('stat-total').textContent = pedagoget.length;
  const depts = new Set(pedagoget.map(p => p.dept));
  document.getElementById('stat-dept').textContent = depts.size;
  const total = pedagoget.reduce((s, p) => s + parseInt(p.kredite || 0), 0);
  document.getElementById('stat-kredite').textContent = total;
}

function updateFilterDept() {
  const sel = document.getElementById('filter-dept');
  const depts = [...new Set(pedagoget.map(p => p.dept))];
  sel.innerHTML = '<option value="">Të gjitha Departamentet</option>' + depts.map(d => `<option>${d}</option>`).join('');
}

// XML
function buildXML() {
  if (pedagoget.length === 0) { showToast('⚠ Nuk ka pedagogë për të eksportuar!', 'error'); return null; }
  const now = new Date().toISOString();
  let xml = `<?xml version="1.0" encoding="UTF-8"?>\n`;
  xml += `<SistemiPedagoget version="2.0" data_gjenerimit="${now}">\n`;
  xml += `  <Metadata>\n`;
  xml += `    <Moduli>Modul 2 - Sistemi i Pedagogëve</Moduli>\n`;
  xml += `    <GjithsejPedagoget>${pedagoget.length}</GjithsejPedagoget>\n`;
  xml += `    <VitiAkademik>${pedagoget[0]?.viti || '2024-2025'}</VitiAkademik>\n`;
  xml += `  </Metadata>\n`;
  xml += `  <Pedagoget>\n`;
  pedagoget.forEach(p => {
    xml += `    <Pedagog id="${p.id}">\n`;
    xml += `      <NrPersoneli>${p.nr}</NrPersoneli>\n`;
    xml += `      <Emri>${p.emri}</Emri>\n`;
    xml += `      <Mbiemri>${p.mbiemri}</Mbiemri>\n`;
    xml += `      <Titulli>${p.titulli}</Titulli>\n`;
    xml += `      <Fakulteti>${p.fakulteti}</Fakulteti>\n`;
    xml += `      <Departamenti>${p.dept}</Departamenti>\n`;
    xml += `      <VitiAkademik>${p.viti}</VitiAkademik>\n`;
    xml += `      <Kredite>${p.kredite}</Kredite>\n`;
    if (p.lendet) {
      xml += `      <Lendet>\n`;
      p.lendet.split(',').forEach(l => {
        xml += `        <Lende>${l.trim()}</Lende>\n`;
      });
      xml += `      </Lendet>\n`;
    }
    if (p.email) xml += `      <Email>${p.email}</Email>\n`;
    xml += `      <DataRegjistrimit>${p.ora}</DataRegjistrimit>\n`;
    xml += `    </Pedagog>\n`;
  });
  xml += `  </Pedagoget>\n`;
  xml += `</SistemiPedagoget>`;
  return xml;
}

function syntaxHighlight(xml) {
  return xml
    .replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')
    .replace(/&lt;(\/?[\w:]+)(.*?)&gt;/g, (m, tag, attrs) => {
      const a = attrs.replace(/(\w+)="([^"]*)"/g, '<span style="color:var(--gold-light)">$1</span>="<span style="color:#a8d8a8">$2</span>"');
      return `<span style="color:#7fc3e8">&lt;${tag}${a}&gt;</span>`;
    });
}

function generateXML() {
  const xml = buildXML();
  if (!xml) return;
  document.getElementById('xml-output').innerHTML = syntaxHighlight(xml);
  document.getElementById('xml-preview').style.display = 'block';
  document.getElementById('xml-empty').style.display = 'none';
  showToast('✓ XML u gjenerua me sukses!');
}

function downloadXML() {
  const xml = buildXML();
  if (!xml) return;
  const blob = new Blob([xml], {type:'application/xml'});
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = 'pedagoget_' + new Date().toISOString().split('T')[0] + '.xml';
  a.click();
  showToast('⬇ Skedari XML u shkarkua!');
}

function copyXML() {
  const xml = buildXML();
  if (!xml) return;
  navigator.clipboard.writeText(xml).then(() => showToast('📋 XML u kopjua në clipboard!'));
}
</script>

</body>
</html>
